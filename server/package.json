{
  "name": "DroneCTRLMockServer",
  "version": "1.0.0",
  "description": "A mock of the web server running on the flight controller's ESP8266 module",
  "main": "./build/index.js",
  "scripts": {
    "prep": "npm i",
    "build": "tsc",
    "start": "node ./build/index.js",
    "start:watch": "tsc -w & nodemon -q -w ./build ./build/index.js",
    "clean": "rimraf ./build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Zachary Heil",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@zackheil/lambda-logger": "^0.1.2",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
